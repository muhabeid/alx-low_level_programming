#include "main.h"

/**
 * _abs(int) - computes the absolute of an integer
 * @n: the int to be checked
 * Return: n if absolute else, -n 
 */
int _abs(int n)
{
  if (n >= 0)
    return (n);
  else
    return (-n);
}